package model;

public enum GameState {
    LOGIN,
    REGISTER,
    MAIN_MENU,
    START,
    CONTROLS,
    SETTINGS,
    QUIT_CONFIRM,
    PLAYING,
    PAUSED,
    LEVEL_LOCKED_POPUP,
    LEVEL_INFO_POPUP
}
